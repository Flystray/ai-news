# planning-with-files：用三个 Markdown 文件，让 AI 编程助手再也不"失忆"

> 📅 2026-06-11 | ⭐ 23,012 | 🔤 Python / MIT | 🔗 [GitHub](https://github.com/OthmanAdi/planning-with-files)

## 项目背景

你在用 Claude Code、Cursor 等AI编程助手时，是不是经常遇到这些尴尬场景：

- 聊着聊着，它就忘了最开始说的目标
- 运行 `/clear` 清上下文，它又得重新理解项目
- 50次工具调用后，它已经偏到不知哪去了
- 同一个报错试了三遍——因为它不记得之前已经失败了

这些问题的根源都是同一个：**AI的记忆是临时的，没有持久化**。上下文窗口就像RAM——快但容量有限，重启就丢。

**planning-with-files** 就是为解决这个问题而生的。它的核心思路来自 Manus AI——那个8个月内从启动到1亿美元营收、2025年12月被Meta以20亿美元收购的AI Agent公司。Manus的核心秘诀就是："Markdown是我的'工作记忆'磁盘。由于我迭代式处理复杂任务，上下文窗口不可避免地会被压缩，所以我把任何重要信息都写入磁盘。"

planning-with-files 将这套思路在Claude Code生态中开源实现，通过持久化的文件系统替代易失的上下文窗口，让AI编程助手从"重启即忘"变成"永久记忆"。截至2026年6月，项目已获得23k+ Stars，支持60+ AI编程平台，v3.0.0新增了autonomous和gated两种运行模式。

## 核心功能

### 1. 三文件持久化记忆系统
将AI的"工作记忆"从易失的RAM转移到持久的磁盘，通过三个职责分明的Markdown文件构成完整的任务执行元认知框架：

| 文件 | 职责 | 更新时机 | 对应问题 |
|------|------|----------|----------|
| `task_plan.md` | 做什么——任务路线图、阶段划分、进度checkbox | 每个阶段完成后 | 目标漂移 |
| `findings.md` | 发现了什么——技术决策、代码库分析、踩坑记录 | 每2次浏览/搜索操作后 | 重复研究 |
| `progress.md` | 做了什么——会话日志、测试结果、操作时间线 | 整个会话期间 | 上下文断裂 |

三文件设计的关键智慧在于"各司其职"：计划管进度，笔记管知识，日志管时间线。下次接手任务时，先看task_plan知道进度，再看findings知道背景，再看progress知道细节——信息检索效率远高于在冗长上下文中翻找。

### 2. 五钩子自动化生命周期
项目利用Claude Code的Hook系统，在关键节点自动触发特定行为，形成闭环：

| Hook | 触发时机 | 核心动作 |
|------|----------|----------|
| `SessionStart` | 启动时 | 注入计划文件内容为系统背景 |
| `UserPromptSubmit` | 用户提交指令前 | 静默注入task_plan前50行 + progress末20行 |
| `PreToolUse` ⭐ | 每次工具调用前 | 截取task_plan前30行，通过stderr"耳语"目标提醒（非阻塞，始终allow） |
| `PostToolUse` | 文件修改后 | 提醒更新progress.md并勾选checkbox |
| `Stop` ⭐ | 想停止时 | 运行check-complete.sh正则扫描，发现未完成则阻断退出（最多3次） |

**PreToolUse是防止"目标漂移"的核心武器**：每次动手之前先看计划，就像导航每到路口都提醒方向。而Stop钩子则是"完成守门员"——不允许AI在任务没做完时擅自停止。

### 3. 会话恢复系统（session-catchup）
当上下文窗口满了执行 `/clear` 或开启新对话时，系统自动检测并生成恢复报告：

```
📋 会话恢复报告
上次活动: 2小时前 | 任务进度: ████████░░ 40%
✅ 已完成: Phase 1, Phase 2
🔄 进行中: Phase 3 (2/4)
💡 最近发现: next-mdx-enhanced 已停止维护
🚧 未解决问题: rehype 插件与 Next.js 冲突
```

session-catchup.py脚本从JSONL会话日志中深层扫描，智能截取自上次task_plan变动后的新对话，以不超过100行的精华摘要重新注入新会话——实现跨会话无缝接续。

### 4. v3.0 新增：autonomous 与 gated 模式
v3.0.0引入了两种运行模式选择：

- **autonomous模式**：AI自主规划并执行，适合长周期自动化任务
- **gated模式**：引入确定性完成守门员（Completion Gate），AI必须等到计划所有阶段都真正标记为complete才能停止——不允许"差不多就停了"
- 默认模式不变，现有设置无需任何更改，两种新模式均为opt-in

### 5. SHA-256 计划认证（attest-plan）
v2.37.0引入的安全机制：对task_plan.md生成SHA-256哈希值，提供密码学保证——防止计划在执行过程中被悄无声息地篡改。将安全机制直接嵌入AI Agent工作流，这是非常前沿的独特实践。

### 6. 多智能体并行计划管理
支持多智能体同时维护不同计划，通过三级优先级级联解析：

```
$MANUS_PROJECT (环境变量，最高优先级)
    ↓
.planning/.active.override.$SESSION_ID (会话级隔离)
    ↓
.planning/index.md 中的 active: 字段 (工作区默认)
```

利用终端唯一会话ID实现天然沙箱隔离，支持自然语言切换（如"switch to backend-api"）。

### 7. 四条黄金行为守则
系统内置了强制性的行为约束，确保AI的执行质量：

| 守则 | 机制 |
|------|------|
| 强制优先规划 | 未创建task_plan.md前禁止写代码 |
| 读而后决 | 架构决策前强制重读task_plan.md |
| 双重行动法则 | 每2次浏览/搜索后必须写入findings.md |
| 三击错误防范 | 错误记录 + 根因分析 + 变异解决方案 |

## 技术亮点

### 1. 文件系统即外部工作记忆的架构范式
将计算机体系结构的内存层次模型映射到AI Agent工作上下文：上下文窗口=RAM（易失、有限、快），文件系统=Disk（持久、无限、稍慢）。核心原则"任何重要信息都写入磁盘"看似朴素，实则是对AI编程最核心痛点的精准打击。对比传统做法——把所有信息塞进上下文窗口导致Token爆炸——planning-with-files的 Hooks只在关键时刻读取，反而更节省Token。

### 2. PreToolUse stderr"耳语"注入机制
不同于UserPromptSubmit的静默注入（占上下文空间），PreToolUse采用了一个巧妙的技术实现：通过标准错误流（stderr）以非阻塞方式注入目标提醒。这种方式不占用正式上下文窗口空间，决策始终为allow（不阻止工具调用），只在AI的"注意力后台"轻声提醒当前目标——实现了真正的注意力刷新而非注意力堆砌。

### 3. 60+ 平台的标准化技能适配
项目通过SKILL.md标准实现了跨平台技能分发，覆盖Claude Code、Codex CLI、Cursor、Gemini CLI、Hermes Agent、Pi Agent、Kiro、OpenCode等60+ AI编程平台。同时提供Unix Shell脚本和Windows PowerShell脚本双版本支持，以及阿拉伯语、德语、西班牙语、简体中文等5种语言的国际化。

## 对比分析

| 维度 | planning-with-files | Claude Code TodoWrite | Manus AI |
|------|---------------------|----------------------|----------|
| 存储位置 | 文件系统（持久） | 上下文（易失） | SaaS平台 |
| 跨会话 | ✅ 无缝恢复 | ❌ 清除即丢 | ✅ 云端持久 |
| 目标漂移防护 | ✅ PreToolUse Hook自动提醒 | ❌ 无机制 | ✅ 内置 |
| 错误追踪 | ✅ 专门错误表+三击防范 | ❌ 无记录 | ✅ 内置 |
| 研究存储 | ✅ findings.md知识库 | ❌ 无机制 | ✅ 内置 |
| 安全认证 | ✅ SHA-256计划哈希 | ❌ 无 | ✅ 企业级 |
| 完成守门 | ✅ Stop Hook+gated模式 | ❌ 无 | ✅ 内置 |
| 多智能体 | ✅ 会话级计划隔离 | ❌ 单会话 | ✅ 多代理 |
| 成本 | 免费（开源MIT） | 免费（内置） | SaaS付费 |
| Benchmark | 96.7% pass rate (v2.21.0) | 无公开数据 | 无公开数据 |

**关键数据**：A/B盲测3/3获胜；SkillCheck验证通过；安全审计通过（v2.21.0）；Benchmark通过率96.7%（sonnet-4-6模型）。

## 适用人群

| 推荐度 | 人群 |
|--------|------|
| ⭐⭐⭐⭐⭐ 强烈推荐 | 经常处理多步骤复杂任务的开发者、需要收集整理大量信息的研究人员、大型重构任务爱好者 |
| ⭐⭐⭐⭐ 推荐 | 团队协作（计划文件可作为沟通文档）、个人知识管理爱好者 |
| ⭐⭐⭐ 可选 | 新手开发者（有学习成本但值得投入）、偶尔使用者 |
| ⭐⭐ 不推荐 | 只做简单任务的用户（杀鸡用牛刀）、追求极速的人 |

**判断标准**：预计工具调用 > 5次 → 用；< 5次 → 不用。

## 快速上手

```bash
# 方法一：插件市场安装（推荐）
/plugin marketplace add OthmanAdi/planning-with-files
/plugin install planning-with-files@planning-with-files

# 方法二：手动安装
mkdir -p .claude/plugins
git clone https://github.com/OthmanAdi/planning-with-files.git .claude/plugins/planning-with-files
```

安装后，在处理复杂任务前运行 `/plan` 即可启动三文件模式。核心命令：

| 命令 | 用途 |
|------|------|
| `/plan` | 初始化或恢复规划会话 |
| `/plan:status` | 查看当前进度报告 |
| `/plan-attest` | SHA-256锁定当前计划 |

**避坑提醒**：① 简单任务别用，增加开销；② 阶段别切太细也别太粗（10-30分钟一个阶段）；③ 有重要发现就更新findings，别等2次操作；④ 定期把完成的文件归档到 `.archive/` 目录。